# 🔧 视频配置页面更新 - 快速修复步骤

## 问题：刷新页面后看不到"文生图 API Key"输入框

## ✅ 立即执行这3步

### 步骤1：停止前端开发服务器

**找到运行前端的终端窗口**（通常显示 `vite` 或端口5173）

```bash
# 按 Ctrl+C 停止前端服务
```

### 步骤2：重启前端

```bash
# 在项目根目录（91Writing）
npm run dev
```

### 步骤3：刷新浏览器

```
按 Ctrl+Shift+R（强制刷新）
```

---

## ✅ 现在应该能看到

火山引擎（文生图Provider）下面应该有**3个输入框**：

```
✅ Access Key ID         [输入框]
✅ Secret Access Key     [密码框]
✅ 文生图 API Key        [密码框] ⭐ 新增的
   └─ 提示：火山引擎文生图需要单独的API Key
```

---

## ✅ 然后填写并测试

1. **填写3个密钥**
   - Access Key ID: AKLT...
   - Secret Access Key: ***
   - 文生图 API Key: *** ⭐

2. **点击"测试火山引擎连接"**
   
   应该提示：
   ```
   ✅ 火山引擎连接正常（临时配置验证通过，请保存配置以永久生效）
   ```

3. **点击"保存配置"**

   应该提示：
   ```
   ✅ 配置保存成功
   ```

---

## 🎯 关键点

**Vite开发服务器会热更新Vue文件，但有时需要手动重启**

如果还看不到新输入框：
1. 确认是否重启了前端 `npm run dev`
2. 确认浏览器是否强制刷新 `Ctrl+Shift+R`
3. 清除浏览器缓存

---

## 📝 完整操作清单

- [ ] 停止前端服务（Ctrl+C）
- [ ] 重启前端服务（npm run dev）
- [ ] 刷新浏览器（Ctrl+Shift+R）
- [ ] 确认看到3个火山引擎输入框
- [ ] 填写所有3个密钥
- [ ] 测试连接
- [ ] 保存配置

---

**如果完成上述步骤后仍然看不到，请截图发给我，我会进一步检查。**

